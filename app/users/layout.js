export const metadata = {
	title: 'Parallel Users Data',
	description: 'fetch asynchronous data in parallel with Promise.all()',
	keywords: 'Async & Promise all'
};

const UsersLayout = ({ children }) => {
	return <>{children}</>;
};

export default UsersLayout;
